{
  "name": "claude-connect",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "web:dev": "pnpm --filter web dev",
    "web:build": "pnpm --filter web build",
    "web:preview": "pnpm --filter web preview",
    "api:start": "pnpm --filter api start",
    "api:dev": "pnpm --filter api start:dev",
    "api:debug": "pnpm --filter api start:debug",
    "api:build": "pnpm --filter api build",
    "api:test": "pnpm --filter api test",
    "api:test:watch": "pnpm --filter api test:watch",
    "api:test:cov": "pnpm --filter api test:cov",
    "api:test:e2e": "pnpm --filter api test:e2e",
    "api:lint": "pnpm --filter api lint",
    "api:format": "pnpm --filter api format",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  }
}
